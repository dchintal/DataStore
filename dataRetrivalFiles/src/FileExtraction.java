import java.io.BufferedInputStream;
import java.io.File;
import java.io.FileInputStream;
import java.io.InputStream;

import org.apache.tools.bzip2.CBZip2InputStream;
public class FileExtraction {
	
	/*public static void main(String args[]) {
		 File actualFile   = project.resolveFile("asf-logo-huge.tar.bz2");	
		 InputStream actualFile =
				             new BufferedInputStream(new FileInputStream(actualFile));
				         assertEquals((byte) 'B', actualFile.read());
				         assertEquals((byte) 'Z', actualFile.read());
				 actualFile = new CBZip2InputStream(actualFile);		 
	}*/
}
